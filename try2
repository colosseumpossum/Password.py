logged_in = False
tries = 3

def wrong_password():  global tries 
  while tries > 0:    
    tries -= 1  
    print (f"Password does not match. {tries} tries left.")   
    global password    
    password = input("Try again:" )      

def correct_password():  
  print (f"You are now logged in! {username}")  
  global logged_in  
  logged_in = True  

print("Would you like to log in or create an account?")
choice = input()
if choice== "log in":  
  username = input("User: ")  
  if username == "Possum":    
    password = input("Password: ")    
    if password == "test":      
      correct_password()    
    else:      
      wrong_password()  
  else:    print("No account found.")
elif choice == "create an account": 
  username = input("What do you want your user to be? ")  
  print("Choose a password and confirm it")  
  password = input("Password: ")  
  guess = input("Confirm password: ")  
  if guess == password:    
    correct_password()  
  else:    wrong_password()
    else:  print("Invalid action")  

if not logged_in:  print ("Error creating/logging into account")
